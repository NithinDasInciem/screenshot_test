PORT=5050

NODE_ENV=development

DB_URI=mongodb://localhost:27017/login

JWT_SECRET=change_this_to_a_strong_secret
JWT_EXPIRES_IN=7d

LOG_LEVEL=info

AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=your_bucket_name

# SMTP basics
SMTP_HOST=smtp.googlemail.com
SMTP_PORT=465
SMTP_SECURE=true          # true only for port 465
SMTP_REQUIRE_TLS=false      # for STARTTLS on 587

# Auth
SMTP_USER=majortest007@gmail.com
SMTP_PASS=
SMTP_FROM=Major No-Replay <majortest007@gmail.com>

# Optional debugging/tuning
SMTP_DEBUG=false
SMTP_VERIFY=true           # set to false to skip verify() on boot
SMTP_POOL=false
SMTP_TLS_REJECT_UNAUTHORIZED=true
SMTP_CONNECTION_TIMEOUT=20000
SMTP_GREETING_TIMEOUT=10000
SMTP_SOCKET_TIMEOUT=20000

FRONTEND_URL=http://localhost:3000
